- dedicatoria
- agradecimentos
  - financiamento pelo cnpq
- resumo
  - palavras chaves

- sumario
- lista de figuras
- lista de tabelas
- lista de quadros

- introducao
- referencial teórico
  - reg logistica(modelo e interpretação)
  - redes neurais
  - shap
  - metódos de avaliação(f1 score, matriz confusão)
- metodologia(materias e métodos)
  - banco de dados(origem dados, característica dos dados, )
  - métodos(referencial teorico aplicado no projeto
      (e.g regressão logistica aplicada no emprestimo, 
        redes neurais(qual modelo usar, arquitetura)))
      (intuito de interpretar o modelo(coefs, teste), tanto tradicional quanto para redes neurais(shap))
      (fazer a contextualização)

- resultados
  - analise exploratoria(boxplot, barras e correlograma)
  - regressao logistica e interpretação
  - redes neurais
    - falar dos outros modelos estimados
    - pegar o melhor modelo de redes neurais
  - interpretação do modelo usando shap
    - mostrar gráfico da média dos shap vs regressao logistica
    - mostrar o grafico de dependencia entre valor da variável com resultado do modelo variando ela
    - mostrar 2 gráficos de shap especificos de 2 observações(pra mau pagador e pra bom pagador)
    - mostrar o gráfico com todos as amostras de shap(shap.plots.beeswarm)
    - mostrar o gráfico de força pra apenas uma observação
    - mostrar o gráfico de força para todas as observações
  - fazer um benchmark entre modelo logistico e rede neural
- conclusao
- referencias
- anexo
